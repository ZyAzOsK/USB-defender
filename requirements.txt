watchdog>=3.0
tabulate>=0.9
cryptography>=41.0
fastapi>=0.110
uvicorn>=0.27
# Required for the /ws/monitor endpoint. Without a WebSocket implementation
# uvicorn answers the upgrade request with 404 and the Live Monitor silently
# never connects — bare `uvicorn` does not pull this in.
websockets>=12.0
pytest>=8.0
httpx>=0.27
pyudev>=0.24;sys_platform=="linux"
# WMI is optional on Windows: usb_detector defaults to dependency-free ctypes
# drive polling, which survives PyInstaller bundling. Install this only if you
# explicitly construct USBDetector(use_wmi=True).
# wmi;sys_platform=="win32"
